"use client";
import Link from "next/link";import {FormEvent,use,useState} from "react";import {GraduationCap,LoaderCircle,LockKeyhole} from "lucide-react";import "../../auth.css";
export default function Reset({params}:{params:Promise<{token:string}>}){const{token}=use(params);const[saving,setSaving]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm")){setError("Las contraseñas no coinciden.");setSaving(false);return}const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:f.get("password")})});const d=await r.json();if(!r.ok){setError(d.error);setSaving(false);return}setDone(true);setSaving(false)}return <main className="auth-page"><section className="auth-visual"><div className="visual-content"><div className="visual-mark"><GraduationCap/></div><h1>Crea una nueva<br/>contraseña segura.</h1><p>Elige una clave única de al menos ocho caracteres.</p></div></section><section className="auth-form-side"><div className="auth-box"><Link className="auth-brand" href="/"><span><GraduationCap/></span>Aula<b>Nova</b></Link><div className="auth-copy"><small>PROTEGE TU CUENTA</small><h2>Nueva contraseña</h2></div>{done?<><div className="form-error" style={{background:"#ebf8f1",color:"#278a56"}}>Tu contraseña fue actualizada correctamente.</div><p className="auth-switch"><Link href="/login">Iniciar sesión</Link></p></>:<form onSubmit={submit}><label>Contraseña<div className="input-wrap"><LockKeyhole/><input name="password" type="password" minLength={8} required/></div></label><label>Confirmar contraseña<div className="input-wrap"><LockKeyhole/><input name="confirm" type="password" minLength={8} required/></div></label>{error&&<p className="form-error">{error}</p>}<button className="auth-submit" disabled={saving}>{saving?<LoaderCircle className="spin"/>:"Guardar contraseña"}</button></form>}</div></section></main>}
