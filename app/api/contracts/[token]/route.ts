import {NextRequest,NextResponse} from "next/server";import {PutObjectCommand} from "@aws-sdk/client-s3";import {prisma} from "@/lib/prisma";import {hashToken} from "@/lib/tokens";import {storageBucket,storageClient} from "@/lib/storage";import {clientIp} from "@/lib/rate-limit";
async function find(raw:string){return prisma.contractAcceptance.findUnique({where:{tokenHash:hashToken(raw)},include:{student:{select:{name:true,accountType:true,guardianName:true}},enrollment:{include:{classroom:{select:{title:true,teacher:{select:{name:true}}}}}}}})}
export async function GET(_:NextRequest,{params}:{params:Promise<{token:string}>}){const {token}=await params,a=await find(token);if(!a)return NextResponse.json({error:"Enlace inválido"},{status:404});return NextResponse.json({contract:{title:a.contractTitle,content:a.contractSnapshot,studentName:a.student.name,signerName:a.student.accountType==="MINOR"?a.student.guardianName:a.student.name,course:a.enrollment.classroom.title,teacher:a.enrollment.classroom.teacher.name,expiresAt:a.expiresAt,signedAt:a.signedAt}})}
export async function POST(request:NextRequest,{params}:{params:Promise<{token:string}>}){const {token}=await params,a=await find(token);if(!a)return NextResponse.json({error:"Enlace inválido"},{status:404});if(a.signedAt)return NextResponse.json({error:"Este contrato ya fue firmado"},{status:409});if(a.expiresAt<new Date())return NextResponse.json({error:"El enlace venció; solicita uno nuevo"},{status:410});const form=await request.formData(),signerName=String(form.get("signerName")||"").trim(),consent=form.get("consent"),signature=form.get("signature"),front=form.get("idFront"),back=form.get("idBack");if(signerName.length<3||consent!=="true"||!(signature instanceof File)||!(front instanceof File)||!(back instanceof File))return NextResponse.json({error:"Completa la firma, aceptación y ambas caras del documento"},{status:400});for(const file of [signature,front,back])if(!["image/png","image/jpeg","image/webp"].includes(file.type)||file.size>8*1024*1024)return NextResponse.json({error:"Las evidencias deben ser imágenes PNG, JPG o WebP de hasta 8 MB"},{status:415});const prefix=`contracts/${a.id}`,keys={signatureKey:`${prefix}/signature.png`,idFrontKey:`${prefix}/id-front-${crypto.randomUUID()}`,idBackKey:`${prefix}/id-back-${crypto.randomUUID()}`},s3=storageClient();await Promise.all([[signature,keys.signatureKey],[front,keys.idFrontKey],[back,keys.idBackKey]].map(async([value,key])=>{const file=value as File;await s3.send(new PutObjectCommand({Bucket:storageBucket(),Key:key as string,Body:new Uint8Array(await file.arrayBuffer()),ContentType:file.type}))}));await prisma.$transaction([prisma.contractAcceptance.update({where:{id:a.id},data:{...keys,signerName,signedAt:new Date(),ipAddress:clientIp(request),userAgent:request.headers.get("user-agent")?.slice(0,500)}}),prisma.enrollment.update({where:{id:a.enrollmentId},data:{status:"ACTIVE"}})]);return NextResponse.json({ok:true})}
