"use client";
import Link from "next/link";import {useRouter} from "next/navigation";import {FormEvent,useState} from "react";import {Eye,EyeOff,GraduationCap,LoaderCircle,LockKeyhole,Mail,Phone,UserRound,UsersRound} from "lucide-react";import "../auth.css";import {readApiResponse} from "@/lib/client-api";
export default function RegisterPage(){const router=useRouter(),[showPassword,setShowPassword]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState(""),[accountType,setAccountType]=useState<"ADULT"|"MINOR">("ADULT");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const data=new FormData(event.currentTarget);if(data.get("password")!==data.get("confirm")){setError("Las contraseñas no coinciden.");setLoading(false);return}const response=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...Object.fromEntries(data),accountType})}),result=await readApiResponse(response);if(!response.ok){setError(String(result.error||"No se pudo crear la cuenta."));setLoading(false);return}router.push("/");router.refresh()}
return <main className="auth-page"><section className="auth-visual register-visual"><div className="visual-content"><div className="visual-mark"><GraduationCap/></div><h1>Tu aprendizaje<br/>comienza aquí.</h1><p>Accede a contenidos, actividades y acompañamiento desde cualquier lugar.</p><div className="benefit"><b>01</b><span><strong>Todo organizado</strong><small>Clases, recursos y entregas en un mismo espacio.</small></span></div><div className="benefit"><b>02</b><span><strong>Protección para menores</strong><small>La cuenta queda vinculada a su madre, padre o responsable.</small></span></div></div></section><section className="auth-form-side"><div className="auth-box register-box"><Link className="auth-brand" href="/"><span><GraduationCap/></span>Aula<b>Nova</b></Link><div className="auth-copy"><small>ÚNETE A LA COMUNIDAD</small><h2>Crea tu cuenta</h2><p>Selecciona el tipo de registro correspondiente.</p></div><form onSubmit={submit}><div className="account-type"><button type="button" className={accountType==="ADULT"?"active":""} onClick={()=>setAccountType("ADULT")}>Cuenta de adulto</button><button type="button" className={accountType==="MINOR"?"active":""} onClick={()=>setAccountType("MINOR")}>Cuenta de menor</button></div><label>Nombre completo<div className="input-wrap"><UserRound/><input name="name" autoComplete="name" minLength={3} required/></div></label><label>Fecha de nacimiento<div className="input-wrap"><input name="birthDate" type="date" required/></div></label><label>Correo electrónico<div className="input-wrap"><Mail/><input name="email" type="email" autoComplete="email" required/></div></label>{accountType==="MINOR"&&<fieldset className="guardian-fields"><legend><UsersRound/>Datos del padre, madre o responsable</legend><label>Nombre completo<input name="guardianName" required/></label><label>Correo electrónico<input name="guardianEmail" type="email" required/></label><label>Teléfono<div className="input-wrap"><Phone/><input name="guardianPhone" required/></div></label><label>Relación con el menor<select name="guardianRelationship" required><option value="">Seleccionar</option><option>Madre</option><option>Padre</option><option>Tutor legal</option><option>Otro responsable</option></select></label></fieldset>}<label>Contraseña<div className="input-wrap"><LockKeyhole/><input name="password" type={showPassword?"text":"password"} autoComplete="new-password" minLength={8} required/><button type="button" onClick={()=>setShowPassword(value=>!value)}>{showPassword?<EyeOff/>:<Eye/>}</button></div></label><label>Confirmar contraseña<div className="input-wrap"><LockKeyhole/><input name="confirm" type={showPassword?"text":"password"} minLength={8} required/></div></label>{error&&<p className="form-error">{error}</p>}<button className="auth-submit" disabled={loading}>{loading?<LoaderCircle className="spin"/>:"Crear mi cuenta"}</button></form><p className="auth-switch">¿Ya tienes una cuenta? <Link href="/login">Iniciar sesión</Link></p></div></section></main>}
