"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {GraduationCap,LoaderCircle,Mail} from "lucide-react";import "../auth.css";
export default function Recover(){const[sending,setSending]=useState(false),[sent,setSent]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSending(true);const f=new FormData(e.currentTarget);await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email")})});setSending(false);setSent(true)}return <main className="auth-page"><section className="auth-visual"><div className="visual-content"><div className="visual-mark"><GraduationCap/></div><h1>Recupera el acceso<br/>a tu aprendizaje.</h1><p>Te enviaremos un enlace seguro y temporal para crear una contraseña nueva.</p></div></section><section className="auth-form-side"><div className="auth-box"><Link className="auth-brand" href="/"><span><GraduationCap/></span>Aula<b>Nova</b></Link><div className="auth-copy"><small>RECUPERACIÓN SEGURA</small><h2>¿Olvidaste tu contraseña?</h2><p>Ingresa el correo asociado a tu cuenta.</p></div>{sent?<div className="form-error" style={{background:"#ebf8f1",color:"#278a56"}}>Si existe una cuenta con ese correo, recibirás un enlace durante los próximos minutos.</div>:<form onSubmit={submit}><label>Correo electrónico<div className="input-wrap"><Mail/><input name="email" type="email" required autoFocus/></div></label><button className="auth-submit" disabled={sending}>{sending?<LoaderCircle className="spin"/>:"Enviar enlace"}</button></form>}<p className="auth-switch"><Link href="/login">Volver al inicio de sesión</Link></p></div></section></main>}
